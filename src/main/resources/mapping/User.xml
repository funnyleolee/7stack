<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userMapping">
	<select id="findUserById" resultType="user" parameterType="_int">
		select
		id id, user_name as userName, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from users 
		where id = #{value}
	</select>
	<select id="findOneUser" resultType="user" parameterType="user">
		select
		id id, user_name as userName, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from users 
		where 1=1
		<if test="id != null">
			and id = #{id}
		</if>
		<if test="@org.apache.commons.lang3.StringUtils@isNotBlank(userName)">
			and user_name = #{userName}
		</if>
		limit 1
	</select>
	<select id="findUserList" resultType="user" parameterType="user">
		select
		id id, user_name as userName, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from users 
		where 1=1
		<if test="id != null">
			and id = #{id}
		</if>
		<if test="@org.apache.commons.lang3.StringUtils@isNotBlank(userName)">
			and user_name = #{userName}
		</if>
	</select>
	<select id="findAllUserList" resultType="java.util.List">
		select
		id id, user_name as userName, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from users
	</select>
	<update id="updateUser" parameterType="user">
		update users set 
		user_name = #{userName},
		email = #{email},
		address = #{address},
		mobile = #{mobile},
		telephone = #{telephone},
		sns = #{sns}
		where id = #{id}
	</update>
	<insert id="insertUser" parameterType="user">
		insert into users 
		(user_name,password,email,address,mobile,telephone,sns)values
		(#{userName},#{password},#{email},#{address},#{mobile},#{telephone},#{sns})
	</insert>
</mapper>