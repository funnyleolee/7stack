<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userMapping">
	<select id="findUserById" resultType="user" parameterType="_int">
		select
		id id, name as name, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from user 
		where id = #{value}
	</select>
	<select id="findOneUser" resultType="user" parameterType="user">
		select
		id id, name as name, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from user 
		where 1=1
		<if test="id != null">
			and id = #{id}
		</if>
		<if test="@org.apache.commons.lang3.StringUtils@isNotBlank(name)">
			and name = #{name}
		</if>
		limit 1
	</select>
	<select id="findUserList" resultType="user" parameterType="user">
		select
		id id, name as name, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from user 
		where 1=1
		<if test="id != null">
			and id = #{id}
		</if>
		<if test="@org.apache.commons.lang3.StringUtils@isNotBlank(name)">
			and name = #{name}
		</if>
	</select>
	<select id="findAllUserList" resultType="java.util.List">
		select
		id id, name as name, password password, email email, address address,
		mobile mobile, telephone telephone, sns sns from user
	</select>
	<update id="updateUser" parameterType="user">
		update user set 
		name = #{name},
		email = #{email},
		address = #{address},
		mobile = #{mobile},
		telephone = #{telephone},
		sns = #{sns}
		where id = #{id}
	</update>
	<insert id="insertUser" parameterType="user">
		insert into user 
		(name,password,email,address,mobile,telephone,sns)values
		(#{name},#{password},#{email},#{address},#{mobile},#{telephone},#{sns})
	</insert>
</mapper>